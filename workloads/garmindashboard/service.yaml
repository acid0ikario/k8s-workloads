apiVersion: v1
kind: Service
metadata:
  name: garmindashboard
  namespace: garmindashboard
spec:
  selector:
    app: garmindashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
